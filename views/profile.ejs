<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Profile</title>
</head>
<body>
    <%- include('common/header'); %>
    <h1> Welcome <%= user.name %></h1>
    <h2> Welcome <%= user.email %></h2>
    <h3> Welcome <%= user.city %></h3>
    <ul>
        <% user.skills.forEach((item)=>{ %>
            <li> <%= item %></li>
        <% }) %>
    </ul>
</body>
</html>